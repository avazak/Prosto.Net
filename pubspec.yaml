name: prosto_net
description: "AmneziaWG VPN Client - Prosto.Net"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  path_provider: ^2.1.1
  file_picker: ^10.2.0
  qr_code_scanner: ^1.0.1
  http: ^1.1.0
  shared_preferences: ^2.2.2
  permission_handler: ^12.0.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

